{
  // Test-only worker configuration. This intentionally mirrors the pinned
  // Cloudflare Computer dofs benchmark instead of creating an HTTP service.
  "name": "computer-storage-benchmark",
  "main": "./worker.ts",
  "compatibility_date": "2026-05-26",
  "durable_objects": {
    "bindings": [
      {
        "name": "BenchmarkStorage",
        "class_name": "BenchmarkStorage"
      }
    ]
  },
  "migrations": [
    {
      "tag": "v1",
      "new_sqlite_classes": ["BenchmarkStorage"]
    }
  ]
}
