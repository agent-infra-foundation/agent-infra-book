# Ephemeral Sandbox

Book series about parallel coding-agent workspaces built around the
Ephemeral Sandbox runtime.

## Volumes

- [Volume I — The Agent Workspace](volume-1/)

Volume I documents the current workspace runtime. Future volumes can cover
broader agent-runtime concerns such as credentials, microVM tenancy, durable
execution, fleet scheduling, and browser-state isolation.
