# Sandbox Books

This section studies the sandbox as an agent infrastructure primitive:

- execution and filesystem boundaries;
- copy-on-write workspaces and snapshots;
- process and network isolation;
- agent-facing tools and protocols;
- provenance, publication, and recovery.

The first series is [`ephemeral-sandbox/`](ephemeral-sandbox/).
